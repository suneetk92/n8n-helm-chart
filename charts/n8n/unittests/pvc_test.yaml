suite: test pvc

templates:
  - pvc.yaml

release:
  name: n8n
  namespace: n8n

chart:
  version: 1.0.0
  appVersion: 1.0.0

set:
  main:
    persistence:
      enabled: true
  worker:
    persistence:
      enabled: true

tests:
  - it: should not create pvc resource for main node when main.count is 1 and existingClaim is set
    set:
      main:
        count: 1
        persistence:
          existingClaim: "test-existing-claim"
      worker:
        count: 1
        persistence:
          existingClaim: "test-existing-claim"
    asserts:
      - hasDocuments:
          count: 0

  - it: should not create pvc resource for main node when main.count is bigger than 1 and existingClaim is not set
    set:
      main:
        count: 2
        persistence:
          existingClaim: ""
      worker:
        count: 2
        persistence:
          existingClaim: ""
    asserts:
      - hasDocuments:
          count: 0

  - it: should create pvc resource for main node when main.count is 1 and existingClaim is not set
    set:
      main:
        count: 1
        persistence:
          volumeName: "test-volume"
          existingClaim: ""
          mountPath: "/home/node/.n8n"
          subPath: ""
          storageClass: "test-storage-class"
          accessMode: ReadWriteOnce
          size: 1Gi
          annotations:
            key-annotation: value-annotation
          labels:
            key-label: value-label
    documentSelector:
      path: metadata.name
      value: n8n-main-persistence
    asserts:
      - equal:
          path: metadata.labels.key-label
          value: value-label
      - equal:
          path: metadata.annotations.key-annotation
          value: value-annotation
      - lengthEqual:
          path: spec.accessModes
          count: 1
      - equal:
          path: spec.accessModes[0]
          value: ReadWriteOnce
      - equal:
          path: spec.resources.requests.storage
          value: 1Gi
      - equal:
          path: spec.storageClassName
          value: test-storage-class

  - it: should create pvc resource for worker node when worker.count is 1 and existingClaim is not set
    set:
      worker:
        count: 1
        persistence:
          volumeName: "test-volume"
          existingClaim: ""
          mountPath: "/home/node/.n8n"
          subPath: ""
          storageClass: "test-storage-class"
          accessMode: ReadWriteOnce
          size: 1Gi
          annotations:
            key-annotation: value-annotation
          labels:
            key-label: value-label
    documentSelector:
      path: metadata.name
      value: n8n-worker-persistence
    asserts:
      - equal:
          path: metadata.labels.key-label
          value: value-label
      - equal:
          path: metadata.annotations.key-annotation
          value: value-annotation
      - lengthEqual:
          path: spec.accessModes
          count: 1
      - equal:
          path: spec.accessModes[0]
          value: ReadWriteOnce
      - equal:
          path: spec.resources.requests.storage
          value: 1Gi
      - equal:
          path: spec.storageClassName
          value: test-storage-class

  - it: should match snapshot of main persistence
    set:
      main:
        count: 1
        persistence:
          enabled: true
      worker:
        persistence:
          enabled: false
    asserts:
      - matchSnapshot: { }

  - it: should match snapshot of main persistence with multiple main node and mode read write many
    set:
      main:
        count: 3
        persistence:
          enabled: true
          accessMode: ReadWriteMany
    asserts:
      - matchSnapshot: { }

  - it: should match snapshot of worker persistence
    set:
      main:
        persistence:
          enabled: false
      worker:
        count: 1
        persistence:
          enabled: true
    asserts:
      - matchSnapshot: { }

  - it: should not create python-packages PVC when persistence is disabled
    set:
      main:
        persistence:
          enabled: false
      worker:
        persistence:
          enabled: false
      taskRunners:
        mode: external
      nodes:
        python:
          persistence:
            enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should not create python-packages PVC when taskRunners.mode is internal
    set:
      main:
        persistence:
          enabled: false
      worker:
        persistence:
          enabled: false
      taskRunners:
        mode: internal
      nodes:
        python:
          persistence:
            enabled: true
    asserts:
      - hasDocuments:
          count: 0

  - it: should not create python-packages PVC when existingClaim is set
    set:
      main:
        persistence:
          enabled: false
      worker:
        persistence:
          enabled: false
      taskRunners:
        mode: external
      nodes:
        python:
          persistence:
            enabled: true
            existingClaim: my-existing-pvc
    asserts:
      - hasDocuments:
          count: 0

  - it: should create python-packages PVC when persistence is enabled and taskRunners.mode is external
    set:
      main:
        persistence:
          enabled: false
      worker:
        persistence:
          enabled: false
      taskRunners:
        mode: external
      nodes:
        python:
          persistence:
            enabled: true
            storageClass: fast-ssd
            accessMode: ReadWriteOnce
            size: 2Gi
            annotations:
              test-key: test-value
    documentSelector:
      path: metadata.name
      value: n8n-python-packages
    asserts:
      - equal:
          path: spec.accessModes[0]
          value: ReadWriteOnce
      - equal:
          path: spec.resources.requests.storage
          value: 2Gi
      - equal:
          path: spec.storageClassName
          value: fast-ssd
      - equal:
          path: metadata.annotations.test-key
          value: test-value

  - it: should create python-packages PVC with ReadWriteMany access mode
    set:
      main:
        persistence:
          enabled: false
      worker:
        persistence:
          enabled: false
      taskRunners:
        mode: external
      nodes:
        python:
          persistence:
            enabled: true
            accessMode: ReadWriteMany
    documentSelector:
      path: metadata.name
      value: n8n-python-packages
    asserts:
      - equal:
          path: spec.accessModes[0]
          value: ReadWriteMany

  - it: should not create community-packages PVC when persistence is disabled
    set:
      main:
        persistence:
          enabled: false
      worker:
        persistence:
          enabled: false
      nodes:
        external:
          packages:
            - "n8n-nodes-evolution-api"
          persistence:
            enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should not create community-packages PVC when packages list is empty
    set:
      main:
        persistence:
          enabled: false
      worker:
        persistence:
          enabled: false
      nodes:
        external:
          packages: []
          persistence:
            enabled: true
    asserts:
      - hasDocuments:
          count: 0

  - it: should not create community-packages PVC when existingClaim is set
    set:
      main:
        persistence:
          enabled: false
      worker:
        persistence:
          enabled: false
      nodes:
        external:
          packages:
            - "n8n-nodes-evolution-api"
          persistence:
            enabled: true
            existingClaim: my-existing-pvc
    asserts:
      - hasDocuments:
          count: 0

  - it: should create community-packages PVC when persistence is enabled and packages are set
    set:
      main:
        persistence:
          enabled: false
      worker:
        persistence:
          enabled: false
      nodes:
        external:
          packages:
            - "n8n-nodes-evolution-api"
          persistence:
            enabled: true
            storageClass: fast-ssd
            accessMode: ReadWriteOnce
            size: 2Gi
            annotations:
              test-key: test-value
    documentSelector:
      path: metadata.name
      value: n8n-community-packages
    asserts:
      - equal:
          path: spec.accessModes[0]
          value: ReadWriteOnce
      - equal:
          path: spec.resources.requests.storage
          value: 2Gi
      - equal:
          path: spec.storageClassName
          value: fast-ssd
      - equal:
          path: metadata.annotations.test-key
          value: test-value

  - it: should create community-packages PVC with ReadWriteMany access mode
    set:
      main:
        persistence:
          enabled: false
      worker:
        persistence:
          enabled: false
      nodes:
        external:
          packages:
            - "n8n-nodes-evolution-api"
          persistence:
            enabled: true
            accessMode: ReadWriteMany
    documentSelector:
      path: metadata.name
      value: n8n-community-packages
    asserts:
      - equal:
          path: spec.accessModes[0]
          value: ReadWriteMany

  - it: should not create community-packages PVC when main.persistence covers community nodes
    set:
      main:
        persistence:
          enabled: true
          existingClaim: my-n8n-pvc
      worker:
        persistence:
          enabled: false
      nodes:
        external:
          packages:
            - "n8n-nodes-evolution-api"
          persistence:
            enabled: true
    asserts:
      - hasDocuments:
          count: 0

  - it: should create community-packages PVC when main.persistence enabled but worker queue mode has no worker persistence
    set:
      main:
        persistence:
          enabled: true
          existingClaim: my-n8n-pvc
      worker:
        mode: queue
        persistence:
          enabled: false
      db:
        type: postgresdb
      nodes:
        external:
          packages:
            - "n8n-nodes-evolution-api"
          persistence:
            enabled: true
    documentSelector:
      path: metadata.name
      value: n8n-community-packages
    asserts:
      - equal:
          path: spec.accessModes[0]
          value: ReadWriteOnce

  - it: should create worker PVC when worker.count is 3 and accessMode is ReadWriteMany
    set:
      main:
        persistence:
          enabled: false
      worker:
        count: 3
        persistence:
          accessMode: ReadWriteMany
    documentSelector:
      path: metadata.name
      value: n8n-worker-persistence
    asserts:
      - equal:
          path: spec.accessModes[0]
          value: ReadWriteMany

  - it: should not create worker PVC when worker.forceToUseStatefulset is true
    set:
      main:
        persistence:
          enabled: false
      worker:
        count: 1
        forceToUseStatefulset: true
        mode: queue
      db:
        type: postgresdb
    asserts:
      - hasDocuments:
          count: 0

  - it: should not create community-packages PVC when main.forceToUseStatefulset is true and main.persistence is disabled
    set:
      main:
        forceToUseStatefulset: true
        persistence:
          enabled: false
      worker:
        persistence:
          enabled: false
      nodes:
        external:
          packages:
            - "n8n-nodes-evolution-api"
          persistence:
            enabled: true
    asserts:
      - hasDocuments:
          count: 0


  # deployment.yaml renders for `count == 1` regardless of accessMode, and mounts this claim.
  # The condition here previously tested accessMode per count, so `count == 1` + ReadWriteMany
  # produced a Deployment referencing a PVC that was never created.
  - it: should create pvc resource for main node when main.count is 1 and accessMode is ReadWriteMany
    set:
      main:
        count: 1
        persistence:
          enabled: true
          existingClaim: ""
          accessMode: ReadWriteMany
      worker:
        persistence:
          enabled: false
    documentSelector:
      path: metadata.name
      value: n8n-main-persistence
    asserts:
      - equal:
          path: spec.accessModes[0]
          value: ReadWriteMany

  - it: should create pvc resource for main node when main.count is bigger than 1 and accessMode is ReadWriteMany
    set:
      main:
        count: 3
        persistence:
          enabled: true
          existingClaim: ""
          accessMode: ReadWriteMany
      worker:
        persistence:
          enabled: false
    documentSelector:
      path: metadata.name
      value: n8n-main-persistence
    asserts:
      - equal:
          path: spec.accessModes[0]
          value: ReadWriteMany

  - it: should not create pvc resource for main node when main.count is bigger than 1 and accessMode is ReadWriteOnce
    set:
      main:
        count: 3
        persistence:
          enabled: true
          existingClaim: ""
          accessMode: ReadWriteOnce
      worker:
        persistence:
          enabled: false
    asserts:
      - hasDocuments:
          count: 0
