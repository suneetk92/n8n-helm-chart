suite: test deployment-mcp-webhook

templates:
  - configmap.yaml
  - deployment-mcp-webhook.yaml

release:
  name: n8n
  namespace: n8n

chart:
  version: 1.0.0
  appVersion: 1.0.0

set:
  webhook:
    count: 2
    mode: queue
  db:
    type: postgresdb

tests:
  - it: should not have webhook deployment when dbtype is sqlite
    set:
      db:
        type: sqlite
    asserts:
      - hasDocuments:
          count: 0
        template: deployment-mcp-webhook.yaml

  - it: should not have webhook deployment when webhook mode is regular
    set:
      webhook:
        mode: regular
    asserts:
      - hasDocuments:
          count: 0
        template: deployment-mcp-webhook.yaml

  - it: should not set replica count when webhook count is set
    set:
      webhook:
        count: 3
    asserts:
      - equal:
          path: spec.replicas
          value: 1
        template: deployment-mcp-webhook.yaml

  - it: should set strategy to Recreate when strategy.type is Recreate
    set:
      strategy:
        type: Recreate
    asserts:
      - equal:
          path: spec.strategy.type
          value: Recreate
        template: deployment-mcp-webhook.yaml

  - it: should set strategy to RollingUpdate when strategy.type is RollingUpdate
    set:
      strategy:
        type: RollingUpdate
        rollingUpdate:
          maxUnavailable: 1
          maxSurge: 1
    asserts:
      - equal:
          path: spec.strategy.type
          value: RollingUpdate
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.strategy.rollingUpdate.maxUnavailable
          value: 1
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.strategy.rollingUpdate.maxSurge
          value: 1
        template: deployment-mcp-webhook.yaml

  - it: should set pod annotations when podAnnotations are set
    set:
      podAnnotations:
        test: test
    asserts:
      - equal:
          path: spec.template.metadata.annotations.test
          value: test
        template: deployment-mcp-webhook.yaml

  - it: should set pod labels when podLabels are set
    set:
      podLabels:
        test: test
    asserts:
      - equal:
          path: spec.template.metadata.labels.test
          value: test
        template: deployment-mcp-webhook.yaml

  - it: should set image pull secrets when imagePullSecrets are set
    set:
      imagePullSecrets:
        - name: fake-image-pull-secret
    asserts:
      - equal:
          path: spec.template.spec.imagePullSecrets[0].name
          value: fake-image-pull-secret
        template: deployment-mcp-webhook.yaml

  - it: should be custom service account when we do not create it
    set:
      serviceAccount:
        create: false
        name: customsa
    asserts:
      - equal:
          path: spec.template.spec.serviceAccountName
          value: customsa
        template: deployment-mcp-webhook.yaml

  - it: should set pod security context when podSecurityContext is set
    set:
      podSecurityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
    asserts:
      - equal:
          path: spec.template.spec.securityContext.runAsUser
          value: 1000
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.securityContext.runAsGroup
          value: 1000
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.securityContext.runAsNonRoot
          value: true
        template: deployment-mcp-webhook.yaml

  - it: should set container security context when securityContext is set
    set:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].securityContext.runAsUser
          value: 1000
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].securityContext.runAsGroup
          value: 1000
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].securityContext.runAsNonRoot
          value: true
        template: deployment-mcp-webhook.yaml

  - it: should use image repository and tag from image when they set
    set:
      image:
        repository: fake-image-repository/fake-image
        tag: fake-tag
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].image
          value: fake-image-repository/fake-image:fake-tag
        template: deployment-mcp-webhook.yaml

  - it: should use default image and chart app version as image tag when tag is not set
    set:
      image:
        tag: ""
    release:
      name: n8n
      namespace: n8n
    chart:
      version: 1.0.0
      appVersion: 0.81.0
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].image
          value: n8nio/n8n:0.81.0
        template: deployment-mcp-webhook.yaml

  - it: should set image pull policy when imagePullPolicy is set
    set:
      image.pullPolicy: Always
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].imagePullPolicy
          value: Always
        template: deployment-mcp-webhook.yaml

  - it: should set command to n8n with webhook arg
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].command
          content:
            n8n
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].args
          content: webhook
        template: deployment-mcp-webhook.yaml

  - it: should set ports when service.port is set
    set:
      service:
        port: 8080
        name: fake-port
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].ports[0].name
          value: fake-port
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].ports[0].containerPort
          value: 8080
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].ports[0].protocol
          value: TCP
        template: deployment-mcp-webhook.yaml

  - it: should set startup probe when webhook.startupProbe is set
    set:
      webhook:
        mcp:
          startupProbe:
            exec:
              command: ["/bin/sh", "-c", "ps aux | grep '[n]8n'"]
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
            timeoutSeconds: 5
            successThreshold: 1
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].startupProbe.exec.command[0]
          value: /bin/sh
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].startupProbe.exec.command[1]
          value: -c
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].startupProbe.exec.command[2]
          value: ps aux | grep '[n]8n'
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].startupProbe.initialDelaySeconds
          value: 10
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].startupProbe.periodSeconds
          value: 5
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].startupProbe.timeoutSeconds
          value: 5
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].startupProbe.failureThreshold
          value: 30
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].startupProbe.successThreshold
          value: 1
        template: deployment-mcp-webhook.yaml

  - it: should set liveness probe when livenessProbe is set
    set:
      livenessProbe:
        httpGet:
          path: /healthz
          port: http
        initialDelaySeconds: 10
        periodSeconds: 10
        timeoutSeconds: 5
        failureThreshold: 3
        successThreshold: 1
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.httpGet.path
          value: /healthz
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.httpGet.port
          value: http
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.initialDelaySeconds
          value: 10
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.periodSeconds
          value: 10
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.timeoutSeconds
          value: 5
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.failureThreshold
          value: 3
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.successThreshold
          value: 1
        template: deployment-mcp-webhook.yaml

  - it: should set liveness probe when webhook.livenessProbe is set
    set:
      webhook:
        mcp:
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.httpGet.path
          value: /healthz
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.httpGet.port
          value: http
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.initialDelaySeconds
          value: 10
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.periodSeconds
          value: 10
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.timeoutSeconds
          value: 5
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.failureThreshold
          value: 3
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].livenessProbe.successThreshold
          value: 1
        template: deployment-mcp-webhook.yaml

  - it: should set readiness probe when readinessProbe is set
    set:
      readinessProbe:
        httpGet:
          path: /healthz/readiness
          port: http
        initialDelaySeconds: 10
        periodSeconds: 10
        timeoutSeconds: 5
        failureThreshold: 3
        successThreshold: 1
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.httpGet.path
          value: /healthz/readiness
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.httpGet.port
          value: http
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.initialDelaySeconds
          value: 10
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.periodSeconds
          value: 10
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.timeoutSeconds
          value: 5
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.failureThreshold
          value: 3
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.successThreshold
          value: 1
        template: deployment-mcp-webhook.yaml

  - it: should set readiness probe when webhook.readinessProbe is set
    set:
      webhook:
        mcp:
          readinessProbe:
            httpGet:
              path: /healthz/readiness
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.httpGet.path
          value: /healthz/readiness
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.httpGet.port
          value: http
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.initialDelaySeconds
          value: 10
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.periodSeconds
          value: 10
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.timeoutSeconds
          value: 5
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.failureThreshold
          value: 3
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].readinessProbe.successThreshold
          value: 1
        template: deployment-mcp-webhook.yaml

  - it: should set resources when resources are set
    set:
      resources:
        requests:
          cpu: 100m
          memory: 100Mi
        limits:
          cpu: 200m
          memory: 200Mi
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].resources.requests.cpu
          value: 100m
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].resources.requests.memory
          value: 100Mi
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].resources.limits.cpu
          value: 200m
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].resources.limits.memory
          value: 200Mi
        template: deployment-mcp-webhook.yaml

  - it: should set resources when webhook.resources are set
    set:
      webhook:
        mcp:
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 200m
              memory: 200Mi
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].resources.requests.cpu
          value: 100m
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].resources.requests.memory
          value: 100Mi
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].resources.limits.cpu
          value: 200m
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].resources.limits.memory
          value: 200Mi
        template: deployment-mcp-webhook.yaml

  - it: should set hiring banner disabled
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_HIRING_BANNER_ENABLED
            value: "false"
        template: deployment-mcp-webhook.yaml

  - it: should set node environment to production
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: NODE_ENV
            value: production
        template: deployment-mcp-webhook.yaml

  - it: should consume the security configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            configMapRef:
              name: n8n-security-configmap
        template: deployment-mcp-webhook.yaml

  - it: should set port environment variable when service.port is set
    set:
      service:
        port: 8080
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_PORT
            value: "8080"
        template: deployment-mcp-webhook.yaml

  - it: should set default locale environment variable when defaultLocale is set
    set:
      defaultLocale: de
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_DEFAULT_LOCALE
            value: de
        template: deployment-mcp-webhook.yaml

  - it: should set timezone environment variable when timezone is set
    set:
      timezone: America/New_York
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: GENERIC_TIMEZONE
            value: America/New_York
        template: deployment-mcp-webhook.yaml

  - it: should set graceful shutdown timeout environment variable when gracefulShutdownTimeout is set
    set:
      gracefulShutdownTimeout: 10
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_GRACEFUL_SHUTDOWN_TIMEOUT
            value: "10"
        template: deployment-mcp-webhook.yaml

  - it: should disable ui for the webhook
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_DISABLE_UI
            value: "true"
        template: deployment-mcp-webhook.yaml

  - it: should set webhook url environment variable when webhook.url is set
    set:
      webhook:
        url: https://example.com
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: WEBHOOK_URL
            value: https://example.com
        template: deployment-mcp-webhook.yaml

  - it: should set postgresdb user environment variable when externalPostgresql.username is set
    set:
      db:
        type: postgresdb
      externalPostgresql:
          username: n8npostgres
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: DB_POSTGRESDB_USER
            value: n8npostgres
        template: deployment-mcp-webhook.yaml

  - it: should set postgresdb password environment variable from external secret with custom key when existingSecretPasswordKey is set
    set:
      db:
        type: postgresdb
      externalPostgresql:
        existingSecretPasswordKey: custom-postgres-password
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: DB_POSTGRESDB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: n8n-postgresql
                key: custom-postgres-password
                optional: true
        template: deployment-mcp-webhook.yaml

  - it: should set postgresdb password environment variable from secret with key postgres-password
    set:
      db:
        type: postgresdb
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: DB_POSTGRESDB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: n8n-postgresql
                key: postgres-password
                optional: true
        template: deployment-mcp-webhook.yaml

  - it: should set postgresdb password environment variable from secret with key postgres-password and with externalPostgresql.existingSecret when externalPostgresql.existingSecret is set
    set:
      db:
        type: postgresdb
      externalPostgresql:
        existingSecret: custom-secret
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: DB_POSTGRESDB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: custom-secret
                key: postgres-password
                optional: true
        template: deployment-mcp-webhook.yaml

  - it: should set postgresdb password environment variable from external secret with custom key when externalPostgresql.existingSecretPasswordKey is set
    set:
      db:
        type: postgresdb
      externalPostgresql:
        existingSecret: custom-secret
        existingSecretPasswordKey: custom-postgres-password
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: DB_POSTGRESDB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: custom-secret
                key: custom-postgres-password
                optional: true
        template: deployment-mcp-webhook.yaml

  - it: should set queue health check active environment variable
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: QUEUE_HEALTH_CHECK_ACTIVE
            value: "true"
        template: deployment-mcp-webhook.yaml

  - it: should set redis username environment variable from secret
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: QUEUE_BULL_REDIS_USERNAME
            valueFrom:
              secretKeyRef:
                name: n8n-redis
                key: redis-username
                optional: true
        template: deployment-mcp-webhook.yaml

  - it: should set redis username environment variable from external secret when externalRedis.existingSecret is set
    set:
      externalRedis:
        existingSecret: custom-secret
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: QUEUE_BULL_REDIS_USERNAME
            valueFrom:
              secretKeyRef:
                name: custom-secret
                key: redis-username
                optional: true
        template: deployment-mcp-webhook.yaml

  - it: should set redis password environment variable from secret
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: QUEUE_BULL_REDIS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: n8n-redis
                key: redis-password
                optional: true
        template: deployment-mcp-webhook.yaml

  - it: should set redis password environment variable from external secret when externalRedis.existingSecret is set
    set:
      externalRedis:
        existingSecret: custom-secret
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: QUEUE_BULL_REDIS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: custom-secret
                key: redis-password
                optional: true
        template: deployment-mcp-webhook.yaml

  - it: should set redis username and password environment variables from external secret with custom keys
    set:
      externalRedis:
        existingSecret: custom-secret
        existingUsernameKey: custom-redis-username
        existingPasswordKey: custom-redis-password
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: QUEUE_BULL_REDIS_USERNAME
            valueFrom:
              secretKeyRef:
                name: custom-secret
                key: custom-redis-username
                optional: true
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: QUEUE_BULL_REDIS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: custom-secret
                key: custom-redis-password
                optional: true
        template: deployment-mcp-webhook.yaml

  - it: should set extra environment variables when extraEnvVars is set
    set:
      extraEnvVars:
        N8N_EXTRA_ENV_VAR: n8nextraenvvar
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_EXTRA_ENV_VAR
            value: n8nextraenvvar
        template: deployment-mcp-webhook.yaml

  - it: should set extra environment variables when webhook.extraEnvVars is set
    set:
      webhook:
        mcp:
          extraEnvVars:
            N8N_EXTRA_ENV_VAR: n8nextraenvvar
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_EXTRA_ENV_VAR
            value: n8nextraenvvar
        template: deployment-mcp-webhook.yaml

  - it: should set valueFrom environment variables when extraEnv is set
    set:
      extraEnv:
        - name: N8N_VALUE_FROM_VAR
          valueFrom:
            secretKeyRef:
              name: my-secret
              key: my-key
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_VALUE_FROM_VAR
            valueFrom:
              secretKeyRef:
                name: my-secret
                key: my-key
        template: deployment-mcp-webhook.yaml

  - it: should set valueFrom environment variables when webhook.mcp.extraEnv is set
    set:
      webhook:
        mcp:
          extraEnv:
            - name: N8N_VALUE_FROM_VAR
              valueFrom:
                secretKeyRef:
                  name: my-secret
                  key: my-key
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_VALUE_FROM_VAR
            valueFrom:
              secretKeyRef:
                name: my-secret
                key: my-key
        template: deployment-mcp-webhook.yaml

  - it: should not set deprecated N8N_RUNNERS_ENABLED environment variable
    asserts:
      - notContains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_RUNNERS_ENABLED
            value: "true"
        template: deployment-mcp-webhook.yaml

  - it: should set n8n runners mode environment variable
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_RUNNERS_MODE
            value: internal
        template: deployment-mcp-webhook.yaml

  - it: should set database configurations from database configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            configMapRef:
              name: n8n-database-configmap
        template: deployment-mcp-webhook.yaml

  - it: should set logging configurations from logging configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            configMapRef:
              name: n8n-logging-configmap
        template: deployment-mcp-webhook.yaml

  - it: should set diagnostics configurations from diagnostics configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            configMapRef:
              name: n8n-diagnostics-configmap
        template: deployment-mcp-webhook.yaml

  - it: should set version notifications configurations from version notifications configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            configMapRef:
              name: n8n-version-notifications-configmap
        template: deployment-mcp-webhook.yaml

  - it: should set public api configurations from public api configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            configMapRef:
              name: n8n-public-api-configmap
        template: deployment-mcp-webhook.yaml

  - it: should set queue configurations from queue configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            configMapRef:
              name: n8n-queue-configmap
        template: deployment-mcp-webhook.yaml

  - it: should set encryption key from encryption key secret
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            secretRef:
              name: n8n-encryption-key-secret-v2
        template: deployment-mcp-webhook.yaml

  - it: should set extra secret names for envFrom when extraSecretNamesForEnvFrom is set
    set:
      extraSecretNamesForEnvFrom:
        - secret1
        - secret2
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            secretRef:
              name: secret1
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            secretRef:
              name: secret2
        template: deployment-mcp-webhook.yaml

  - it: should set extra secret names for envFrom when webhook.extraSecretNamesForEnvFrom is set
    set:
      webhook:
        mcp:
          extraSecretNamesForEnvFrom:
            - secret1
            - secret2
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            secretRef:
              name: secret1
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            secretRef:
              name: secret2
        template: deployment-mcp-webhook.yaml

  - it: should set extra volume mounts when volumeMounts is set
    set:
      volumeMounts:
        - name: fake-volume
          mountPath: /fake-path
          readOnly: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].volumeMounts
          content:
            name: fake-volume
            mountPath: /fake-path
            readOnly: true
        template: deployment-mcp-webhook.yaml

  - it: should set extra volume mounts when webhook.volumeMounts is set
    set:
      webhook:
        mcp:
          volumeMounts:
            - name: fake-volume
              mountPath: /fake-path
              readOnly: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].volumeMounts
          content:
            name: fake-volume
            mountPath: /fake-path
            readOnly: true
        template: deployment-mcp-webhook.yaml

  - it: should set extra volumes when volumes is set
    set:
      volumes:
        - name: foo
          secret:
            secretName: mysecret
            optional: false
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: foo
            secret:
              secretName: mysecret
              optional: false
        template: deployment-mcp-webhook.yaml

  - it: should set extra volumes when wevhook.volumes is set
    set:
      webhook:
        mcp:
          volumes:
            - name: foo
              secret:
                secretName: mysecret
                optional: false
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: foo
            secret:
              secretName: mysecret
              optional: false
        template: deployment-mcp-webhook.yaml

  - it: should set CA certificate volume and volumeMount when db.type postgresdb, postgresdb.ssl.enabled, and postgresdb.ssl.base64EncodedCertificateAuthorityFile are set
    set:
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            base64EncodedCertificateAuthorityFile: Q0EgZmlsZSBjb250ZW50
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].volumeMounts
          content:
            name: pg-ssl-ca
            mountPath: /home/node/certs/ca/ca.crt
            subPath: ca.crt
            readOnly: true
          any: true
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pg-ssl-ca
            secret:
              secretName: n8n-pg-ssl-ca
              items:
                - key: ca.crt
                  path: ca.crt
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should set CA certificate volume and volumeMount when db.type postgresdb, postgresdb.ssl.enabled, and postgresdb.ssl.existingCertificateAuthorityFileSecret.name are set
    set:
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            existingCertificateAuthorityFileSecret:
              name: ca-cert-secret
              key: root.cert
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].volumeMounts
          content:
            name: pg-ssl-ca
            mountPath: /home/node/certs/ca/ca.crt
            subPath: ca.crt
            readOnly: true
          any: true
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pg-ssl-ca
            secret:
              secretName: ca-cert-secret
              items:
                - key: root.cert
                  path: ca.crt
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should set certificate volume and volumeMount when db.type postgresdb, postgresdb.ssl.enabled, and postgresdb.ssl.base64EncodedCertFile are set
    set:
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            base64EncodedCertFile: Y2VydGlmaWNhdGUgZmlsZSBjb250ZW50
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].volumeMounts
          content:
            name: pg-ssl-cert
            mountPath: /home/node/certs/cert/cert.crt
            subPath: cert.crt
            readOnly: true
          any: true
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pg-ssl-cert
            secret:
              secretName: n8n-pg-ssl-cert
              items:
                - key: cert.crt
                  path: cert.crt
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should set certificate volume and volumeMount when db.type postgresdb, postgresdb.ssl.enabled, and postgresdb.ssl.existingCertFileSecret.name are set
    set:
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            existingCertFileSecret:
              name: my-cert-secret
              key: client.cert
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].volumeMounts
          content:
            name: pg-ssl-cert
            mountPath: /home/node/certs/cert/cert.crt
            subPath: cert.crt
            readOnly: true
          any: true
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pg-ssl-cert
            secret:
              secretName: my-cert-secret
              items:
                - key: client.cert
                  path: cert.crt
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should set certificate private key volume and volumeMount when db.type postgresdb, postgresdb.ssl.enabled, and postgresdb.ssl.base64EncodedPrivateKeyFile are set
    set:
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            base64EncodedPrivateKeyFile: Y2VydGlmaWNhdGUgcHJpdmF0ZSBrZXkgZmlsZSBjb250ZW50
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].volumeMounts
          content:
            name: pg-ssl-key
            mountPath: /home/node/certs/key/cert.key
            subPath: cert.key
            readOnly: true
          any: true
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pg-ssl-key
            secret:
              secretName: n8n-pg-ssl-key
              items:
                - key: cert.key
                  path: cert.key
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should set certificate private key volume and volumeMount when db.type postgresdb, postgresdb.ssl.enabled, and postgresdb.ssl.existingPrivateKeyFileSecret.name are set
    set:
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            existingPrivateKeyFileSecret:
              name: my-cert-private-key-secret
              key: client.key
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].volumeMounts
          content:
            name: pg-ssl-key
            mountPath: /home/node/certs/key/cert.key
            subPath: cert.key
            readOnly: true
          any: true
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pg-ssl-key
            secret:
              secretName: my-cert-private-key-secret
              items:
                - key: client.key
                  path: cert.key
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should set node selector when nodeSelector is set
    set:
      nodeSelector:
        disktype: ssd
    asserts:
      - equal:
          path: spec.template.spec.nodeSelector.disktype
          value: ssd
        template: deployment-mcp-webhook.yaml

  - it: should set affinity when affinity is set
    set:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 1
              preference:
                matchExpressions:
                  - key: disktype
                    operator: In
                    values:
                      - ssd
    asserts:
      - contains:
          path: spec.template.spec.affinity.nodeAffinity.preferredDuringSchedulingIgnoredDuringExecution
          content:
            weight: 1
            preference:
              matchExpressions:
                - key: disktype
                  operator: In
                  values:
                    - ssd
        template: deployment-mcp-webhook.yaml

  - it: should set affinity when webhook.affinity is set
    set:
      webhook:
        mcp:
          affinity:
            podAntiAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
              - labelSelector:
                  matchLabels:
                    app.kubernetes.io/instance: n8n
                    app.kubernetes.io/name: n8n-mcp-webhook
                    app.kubernetes.io/component: mcp-webhook
                topologyKey: kubernetes.io/hostname
    asserts:
      - contains:
          path: spec.template.spec.affinity.podAntiAffinity.requiredDuringSchedulingIgnoredDuringExecution
          content:
            labelSelector:
              matchLabels:
                app.kubernetes.io/instance: n8n
                app.kubernetes.io/name: n8n-mcp-webhook
                app.kubernetes.io/component: mcp-webhook
            topologyKey: kubernetes.io/hostname
        template: deployment-mcp-webhook.yaml

  - it: should set tolerations when tolerations are set
    set:
      tolerations:
        - key: fake-key
          operator: fake-operator
          value: fake-value
    asserts:
      - contains:
          path: spec.template.spec.tolerations
          content:
            key: fake-key
            operator: fake-operator
            value: fake-value
        template: deployment-mcp-webhook.yaml

  - it: should not set deprecated N8N_RUNNERS_ENABLED environment variable in task runner section
    asserts:
      - notContains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_RUNNERS_ENABLED
            value: "true"
        template: deployment-mcp-webhook.yaml

  - it: should set task runner mode environment variable
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_RUNNERS_MODE
            value: internal
        template: deployment-mcp-webhook.yaml

  - it: should set task runner timeout environment variable
    set:
      taskRunners:
        taskTimeout: 30
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_RUNNERS_TASK_TIMEOUT
            value: "30"
        template: deployment-mcp-webhook.yaml

  - it: should set task runner heartbeat interval environment variable
    set:
      taskRunners:
        taskHeartbeatInterval: 15
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_RUNNERS_HEARTBEAT_INTERVAL
            value: "15"
        template: deployment-mcp-webhook.yaml

  - it: should set task runner max concurrency environment variable
    set:
      taskRunners:
        maxConcurrency: 2
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].env
          content:
            name: N8N_RUNNERS_MAX_CONCURRENCY
            value: "2"
        template: deployment-mcp-webhook.yaml

  - it: should use license configmap and secret when license.enabled is true
    set:
      license:
        enabled: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            configMapRef:
              name: n8n-license-configmap
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].envFrom
          content:
            secretRef:
              name: n8n-license-activation-key
        template: deployment-mcp-webhook.yaml

  - it: should use init container to wait main node ready when webhook.waitMainNodeReady.enabled is true
    set:
      webhook:
        waitMainNodeReady:
          enabled: true
    asserts:
      - contains:
          path: spec.template.spec.initContainers
          content:
            name: wait-for-main
            image: busybox:1.36
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should use init container to wait main node ready with http schema when overwriteSchema is not set
    set:
      webhook:
        waitMainNodeReady:
          enabled: true
    asserts:
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "wait-for-main")].args[0]
          value: |
            until wget -q --spider http://n8n.n8n.svc.cluster.local:5678/healthz; do
              echo "Waiting for main node to be ready..."
              sleep 2
            done
        template: deployment-mcp-webhook.yaml

  - it: should use init container to wait main node ready from main N8N_PROTOCOL environment variable when overwriteSchema is not set
    set:
      main:
        extraEnvVars:
          N8N_PROTOCOL: https
      webhook:
        waitMainNodeReady:
          enabled: true
    asserts:
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "wait-for-main")].args[0]
          value: |
            until wget -q --spider https://n8n.n8n.svc.cluster.local:5678/healthz; do
              echo "Waiting for main node to be ready..."
              sleep 2
            done
        template: deployment-mcp-webhook.yaml

  - it: should use init container to wait main node ready with https schema when overwriteSchema is set https
    set:
      webhook:
        waitMainNodeReady:
          enabled: true
          overwriteSchema: https
    asserts:
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "wait-for-main")].args[0]
          value: |
            until wget -q --spider https://n8n.n8n.svc.cluster.local:5678/healthz; do
              echo "Waiting for main node to be ready..."
              sleep 2
            done
        template: deployment-mcp-webhook.yaml

  - it: should use init container to wait main node ready with different service name when overwriteUrl is set
    set:
      webhook:
        waitMainNodeReady:
          enabled: true
          overwriteUrl: "n8n-different-name.n8n.svc.cluster.local:1234"
    asserts:
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "wait-for-main")].args[0]
          value: |
            until wget -q --spider http://n8n-different-name.n8n.svc.cluster.local:1234/healthz; do
              echo "Waiting for main node to be ready..."
              sleep 2
            done
        template: deployment-mcp-webhook.yaml

  - it: should use init container to wait main node ready with different health check path when healthCheckPath is set different
    set:
      webhook:
        waitMainNodeReady:
          enabled: true
          healthCheckPath: "/healthz-different"
    asserts:
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "wait-for-main")].args[0]
          value: |
            until wget -q --spider http://n8n.n8n.svc.cluster.local:5678/healthz-different; do
              echo "Waiting for main node to be ready..."
              sleep 2
            done
        template: deployment-mcp-webhook.yaml

  - it: should use init container to wait main node ready with parameters when additionalParameters is set
    set:
      main:
        extraEnvVars:
          N8N_PROTOCOL: https
      webhook:
        waitMainNodeReady:
          enabled: true
          additionalParameters:
            - "--no-check-certificate"
    asserts:
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "wait-for-main")].args[0]
          value: |
            until wget -q --spider --no-check-certificate https://n8n.n8n.svc.cluster.local:5678/healthz; do
              echo "Waiting for main node to be ready..."
              sleep 2
            done
        template: deployment-mcp-webhook.yaml

  - it: should set security context on wait-for-main init container when webhook.waitMainNodeReady.enabled is true
    set:
      webhook:
        waitMainNodeReady:
          enabled: true
      waitContainerSecurityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
    asserts:
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "wait-for-main")].securityContext.runAsUser
          value: 1000
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "wait-for-main")].securityContext.runAsGroup
          value: 1000
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "wait-for-main")].securityContext.runAsNonRoot
          value: true
        template: deployment-mcp-webhook.yaml

  - it: should set seccompProfile RuntimeDefault in pod security context by default
    asserts:
      - equal:
          path: spec.template.spec.securityContext.seccompProfile.type
          value: RuntimeDefault
        template: deployment-mcp-webhook.yaml

  - it: should always include tmp emptyDir volume
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: tmp
            emptyDir: {}
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should always mount tmp in mcp-webhook container
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].volumeMounts
          content:
            name: tmp
            mountPath: /tmp
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should always include cache emptyDir volume
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: cache
            emptyDir: {}
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should always mount cache in mcp-webhook container
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].volumeMounts
          content:
            name: cache
            mountPath: /home/node/.cache
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should always include data emptyDir volume
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: data
            emptyDir: {}
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should always mount data in mcp-webhook container
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].volumeMounts
          content:
            name: data
            mountPath: /home/node/.n8n
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should use startupProbe to wait n8n process ready
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].startupProbe.exec.command
          value: ["/bin/sh", "-c", "ps aux | grep '[n]8n'"]
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].startupProbe.initialDelaySeconds
          value: 10
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].startupProbe.periodSeconds
          value: 5
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-mcp-webhook")].startupProbe.failureThreshold
          value: 30
        template: deployment-mcp-webhook.yaml

  - it: should add init container to webhook pod when webhook.initContainers is defined
    set:
      webhook:
        mcp:
          initContainers:
            - name: my-init-container
              image: "my-image:1.0.0"
              imagePullPolicy: IfNotPresent
              command:
                - sleep
                - infinity
    asserts:
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "my-init-container")].name
          value: my-init-container
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "my-init-container")].image
          value: "my-image:1.0.0"
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "my-init-container")].imagePullPolicy
          value: IfNotPresent
        template: deployment-mcp-webhook.yaml
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "my-init-container")].command
          value:
            - sleep
            - infinity
        template: deployment-mcp-webhook.yaml

  - it: should add extra containers to webhook pod when webhook.extraContainers is defined
    set:
      webhook:
        mcp:
          extraContainers:
            - name: my-extra-container
              image: "my-image:1.0.0"
              imagePullPolicy: IfNotPresent
              command:
                - sleep
                - infinity
    asserts:
      - contains:
          path: spec.template.spec.containers
          content:
            name: my-extra-container
            image: "my-image:1.0.0"
            imagePullPolicy: IfNotPresent
            command:
              - sleep
              - infinity
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should set dnsPolicy when dnsPolicy is set
    set:
      dnsPolicy: ClusterFirst
    asserts:
      - equal:
          path: spec.template.spec.dnsPolicy
          value: ClusterFirst
        template: deployment-mcp-webhook.yaml

  - it: should set dnsConfig when dnsConfig is set
    set:
      dnsConfig:
        nameservers:
          - 1.1.1.1
          - 1.0.0.1
    asserts:
      - contains:
          path: spec.template.spec.dnsConfig.nameservers
          content:
            1.1.1.1
          any: true
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.dnsConfig.nameservers
          content:
            1.0.0.1
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should set dnsPolicy and dnsConfig when dnsPolicy and dnsConfig are set
    set:
      dnsPolicy: None
      dnsConfig:
        nameservers:
          - 1.1.1.1
          - 1.0.0.1
    asserts:
      - equal:
          path: spec.template.spec.dnsPolicy
          value: None
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.dnsConfig.nameservers
          content:
            1.1.1.1
          any: true
        template: deployment-mcp-webhook.yaml
      - contains:
          path: spec.template.spec.dnsConfig.nameservers
          content:
            1.0.0.1
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should add hostAliases to webhook pod when webhook.hostAliases is defined
    set:
      webhook:
        mcp:
          hostAliases:
            - ip: 1.1.1.1
              hostnames:
                - "my-host.com"
    asserts:
      - contains:
          path: spec.template.spec.hostAliases
          content:
            ip: 1.1.1.1
            hostnames:
              - "my-host.com"
          any: true
        template: deployment-mcp-webhook.yaml

  - it: should set runtimeClassName when webhook.runtimeClassName is defined
    set:
      webhook:
        runtimeClassName: "my-runtime-class"
    asserts:
      - equal:
          path: spec.template.spec.runtimeClassName
          value: my-runtime-class
        template: deployment-mcp-webhook.yaml

  - it: should match snapshot of postgres ssl certificate values
    release:
      name: n8n
      namespace: n8n
    chart:
      version: 1.0.0
      appVersion: 1.0.0
    set:
      encryptionKey: "test-encryption-key"
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            existingCertificateAuthorityFileSecret:
              name: my-ca-cert-secret
              key: root.crt
            existingCertFileSecret:
              name: my-client-cert-secret
              key: client.crt
            existingPrivateKeyFileSecret:
              name: my-client-cert-private-key-secret
              key: client.key
    asserts:
      - matchSnapshot: { }

  - it: should not set revisionHistoryLimit when revisionHistoryLimit is not set
    asserts:
      - notExists:
          path: spec.revisionHistoryLimit
        template: deployment-mcp-webhook.yaml

  - it: should set revisionHistoryLimit when revisionHistoryLimit is set
    set:
      revisionHistoryLimit: 3
    asserts:
      - equal:
          path: spec.revisionHistoryLimit
          value: 3
        template: deployment-mcp-webhook.yaml

  - it: should set revisionHistoryLimit to 0 when revisionHistoryLimit is 0
    set:
      revisionHistoryLimit: 0
    asserts:
      - equal:
          path: spec.revisionHistoryLimit
          value: 0
        template: deployment-mcp-webhook.yaml
