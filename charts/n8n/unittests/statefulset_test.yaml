suite: test statefulset

templates:
  - configmap.yaml
  - secret.yaml
  - statefulset.yaml

release:
  name: n8n
  namespace: n8n

chart:
  version: 1.0.0
  appVersion: 1.0.0

set:
  main:
    count: 2
    persistence:
      enabled: true

tests:
  - it: should be custom service account when we do not create it
    set:
      serviceAccount:
        create: false
        name: customsa
    asserts:
      - equal:
          path: spec.template.spec.serviceAccountName
          value: customsa
        template: statefulset.yaml

  - it: should set pod annotations when podAnnotations are set
    set:
      podAnnotations:
        test: test
    asserts:
      - equal:
          path: spec.template.metadata.annotations.test
          value: test
        template: statefulset.yaml

  - it: should set pod labels when podLabels are set
    set:
      podLabels:
        test: test
    asserts:
      - equal:
          path: spec.template.metadata.labels.test
          value: test
        template: statefulset.yaml

  - it: should set image pull secrets when imagePullSecrets are set
    set:
      imagePullSecrets:
        - name: fake-image-pull-secret
    asserts:
      - equal:
          path: spec.template.spec.imagePullSecrets[0].name
          value: fake-image-pull-secret
        template: statefulset.yaml

  - it: should set pod security context when podSecurityContext is set
    set:
      podSecurityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
    asserts:
      - equal:
          path: spec.template.spec.securityContext.runAsUser
          value: 1000
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.securityContext.runAsGroup
          value: 1000
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.securityContext.runAsNonRoot
          value: true
        template: statefulset.yaml

  - it: should set container security context when securityContext is set
    set:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].securityContext.runAsUser
          value: 1000
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].securityContext.runAsGroup
          value: 1000
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].securityContext.runAsNonRoot
          value: true
        template: statefulset.yaml

  - it: should use image repository and tag from image when they set
    set:
      image:
        repository: fake-image-repository/fake-image
        tag: fake-tag
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].image
          value: fake-image-repository/fake-image:fake-tag
        template: statefulset.yaml

  - it: should use default image and chart app version as image tag when tag is not set
    set:
      image:
        tag: ""
    release:
      name: outline
      namespace: outline
    chart:
      version: 1.0.0
      appVersion: 0.81.0
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].image
          value: n8nio/n8n:0.81.0
        template: statefulset.yaml

  - it: should set image pull policy when imagePullPolicy is set
    set:
      image.pullPolicy: Always
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].imagePullPolicy
          value: Always
        template: statefulset.yaml

  - it: should set ports when service.port is set
    set:
      service:
        port: 8080
        name: fake-port
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].ports[0].name
          value: fake-port
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].ports[0].containerPort
          value: 8080
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].ports[0].protocol
          value: TCP
        template: statefulset.yaml

  - it: should set liveness probe when livenessProbe is set
    set:
      livenessProbe:
        httpGet:
          path: /healthz
          port: http
        initialDelaySeconds: 10
        periodSeconds: 10
        timeoutSeconds: 5
        failureThreshold: 3
        successThreshold: 1
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.httpGet.path
          value: /healthz
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.httpGet.port
          value: http
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.initialDelaySeconds
          value: 10
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.periodSeconds
          value: 10
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.timeoutSeconds
          value: 5
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.failureThreshold
          value: 3
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.successThreshold
          value: 1
        template: statefulset.yaml

  - it: should set liveness probe when main.livenessProbe is set
    set:
      main:
        livenessProbe:
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
          successThreshold: 1
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.httpGet.path
          value: /healthz
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.httpGet.port
          value: http
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.initialDelaySeconds
          value: 10
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.periodSeconds
          value: 10
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.timeoutSeconds
          value: 5
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.failureThreshold
          value: 3
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].livenessProbe.successThreshold
          value: 1
        template: statefulset.yaml

  - it: should set readiness probe when readinessProbe is set
    set:
      readinessProbe:
        httpGet:
          path: /healthz/readiness
          port: http
        initialDelaySeconds: 10
        periodSeconds: 10
        timeoutSeconds: 5
        failureThreshold: 3
        successThreshold: 1
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.httpGet.path
          value: /healthz/readiness
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.httpGet.port
          value: http
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.initialDelaySeconds
          value: 10
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.periodSeconds
          value: 10
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.timeoutSeconds
          value: 5
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.failureThreshold
          value: 3
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.successThreshold
          value: 1
        template: statefulset.yaml

  - it: should set readiness probe when main.readinessProbe is set
    set:
      main:
        readinessProbe:
          httpGet:
            path: /healthz/readiness
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
          successThreshold: 1
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.httpGet.path
          value: /healthz/readiness
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.httpGet.port
          value: http
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.initialDelaySeconds
          value: 10
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.periodSeconds
          value: 10
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.timeoutSeconds
          value: 5
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.failureThreshold
          value: 3
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].readinessProbe.successThreshold
          value: 1
        template: statefulset.yaml

  - it: should set resources when resources are set
    set:
      resources:
        requests:
          cpu: 100m
          memory: 100Mi
        limits:
          cpu: 200m
          memory: 200Mi
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].resources.requests.cpu
          value: 100m
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].resources.requests.memory
          value: 100Mi
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].resources.limits.cpu
          value: 200m
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].resources.limits.memory
          value: 200Mi
        template: statefulset.yaml

  - it: should set resources when main.resources are set
    set:
      main:
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
          limits:
            cpu: 200m
            memory: 200Mi
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].resources.requests.cpu
          value: 100m
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].resources.requests.memory
          value: 100Mi
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].resources.limits.cpu
          value: 200m
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].resources.limits.memory
          value: 200Mi
        template: statefulset.yaml

  - it: should set hiring banner disabled
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_HIRING_BANNER_ENABLED
            value: "false"
        template: statefulset.yaml

  - it: should set node environment to production
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: NODE_ENV
            value: production
        template: statefulset.yaml

  - it: should consume the security configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            configMapRef:
              name: n8n-security-configmap
        template: statefulset.yaml

  - it: should set port environment variable when service.port is set
    set:
      service:
        port: 8080
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_PORT
            value: "8080"
        template: statefulset.yaml

  - it: should set default locale environment variable when defaultLocale is set
    set:
      defaultLocale: de
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_DEFAULT_LOCALE
            value: de
        template: statefulset.yaml

  - it: should set timezone environment variable when timezone is set
    set:
      timezone: America/New_York
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: GENERIC_TIMEZONE
            value: America/New_York
        template: statefulset.yaml

  - it: should set graceful shutdown timeout environment variable when gracefulShutdownTimeout is set
    set:
      gracefulShutdownTimeout: 10
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_GRACEFUL_SHUTDOWN_TIMEOUT
            value: "10"
        template: statefulset.yaml

  - it: should set postgresdb user environment variable when db type is postgresdb and postgresql.enabled is true and postgresql.auth.username is set
    set:
      db:
        type: postgresdb
      postgresql:
        enabled: true
        auth:
          username: n8npostgres
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: DB_POSTGRESDB_USER
            value: n8npostgres
        template: statefulset.yaml

  - it: should set postgresdb user environment variable when db type is postgresdb and postgresql.enabled is false and externalPostgresql.username is set
    set:
      db:
        type: postgresdb
      postgresql:
        enabled: false
      externalPostgresql:
          username: n8npostgres
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: DB_POSTGRESDB_USER
            value: n8npostgres
        template: statefulset.yaml

  - it: should set postgresdb password environment variable from secret with key password when db type is postgresdb and postgresql.enabled is true and postgresql.auth.password is set
    set:
      db:
        type: postgresdb
      postgresql:
        enabled: true
        auth:
          username: n8npostgres
          password: n8npostgrespassword
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: DB_POSTGRESDB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: n8n-postgresql
                key: password
                optional: true
        template: statefulset.yaml

  - it: should set postgresdb password environment variable from external secret with custom key when db type is postgresdb and postgresql.auth.username is set and existingSecretPasswordKey is set
    set:
      db:
        type: postgresdb
      postgresql:
        enabled: true
        auth:
          username: n8npostgres
          password: n8npostgrespassword
      externalPostgresql:
        existingSecretPasswordKey: custom-postgres-password
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: DB_POSTGRESDB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: n8n-postgresql
                key: custom-postgres-password
                optional: true
        template: statefulset.yaml

  - it: should set postgresdb password environment variable from secret with key postgres-password when db type is postgresdb and postgresql.auth.username is not set
    set:
      db:
        type: postgresdb
      postgresql:
        enabled: true
        auth:
          username: ""
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: DB_POSTGRESDB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: n8n-postgresql
                key: postgres-password
                optional: true
        template: statefulset.yaml

  - it: should set postgresdb password environment variable from secret with key postgres-password and with externalPostgresql.existingSecret when db type is postgresdb and externalPostgresql.existingSecret is set
    set:
      db:
        type: postgresdb
      externalPostgresql:
        existingSecret: custom-secret
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: DB_POSTGRESDB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: custom-secret
                key: postgres-password
                optional: true
        template: statefulset.yaml

  - it: should set postgresdb password environment variable from external secret with custom key when db type is postgresdb and externalPostgresql.existingSecretPasswordKey is set
    set:
      db:
        type: postgresdb
      externalPostgresql:
        existingSecret: custom-secret
        existingSecretPasswordKey: custom-postgres-password
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: DB_POSTGRESDB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: custom-secret
                key: custom-postgres-password
                optional: true
        template: statefulset.yaml

  - it: should set redis username environment variable when worker mode is queue and db type is postgresdb
    set:
      worker:
        mode: queue
      db:
        type: postgresdb
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: QUEUE_BULL_REDIS_USERNAME
            valueFrom:
              secretKeyRef:
                name: n8n-redis
                key: redis-username
                optional: true
        template: statefulset.yaml

  - it: should set redis username environment variable when worker mode is queue and db type is postgresdb and externalRedis.existingSecret is set
    set:
      worker:
        mode: queue
      db:
        type: postgresdb
      externalRedis:
        existingSecret: custom-secret
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: QUEUE_BULL_REDIS_USERNAME
            valueFrom:
              secretKeyRef:
                name: custom-secret
                key: redis-username
                optional: true
        template: statefulset.yaml

  - it: should set redis password environment variable when worker mode is queue and db type is postgresdb
    set:
      worker:
        mode: queue
      db:
        type: postgresdb
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: QUEUE_BULL_REDIS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: n8n-redis
                key: redis-password
                optional: true
        template: statefulset.yaml

  - it: should set redis password environment variable when worker mode is queue and db type is postgresdb and externalRedis.existingSecret is set
    set:
      worker:
        mode: queue
      db:
        type: postgresdb
      externalRedis:
        existingSecret: custom-secret
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: QUEUE_BULL_REDIS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: custom-secret
                key: redis-password
                optional: true
        template: statefulset.yaml

  - it: should set redis username and password environment variables from external secret with custom keys when worker mode is queue and db type is postgresdb
    set:
      worker:
        mode: queue
      db:
        type: postgresdb
      externalRedis:
        existingSecret: custom-secret
        existingUsernameKey: custom-redis-username
        existingPasswordKey: custom-redis-password
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: QUEUE_BULL_REDIS_USERNAME
            valueFrom:
              secretKeyRef:
                name: custom-secret
                key: custom-redis-username
                optional: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: QUEUE_BULL_REDIS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: custom-secret
                key: custom-redis-password
                optional: true
        template: statefulset.yaml

  - it: should set webhook url environment variable when webhook.url is set
    set:
      webhook:
        url: https://example.com
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: WEBHOOK_URL
            value: https://example.com
        template: statefulset.yaml

  - it: should set extra environment variables when extraEnvVars is set
    set:
      extraEnvVars:
        N8N_EXTRA_ENV_VAR: n8nextraenvvar
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_EXTRA_ENV_VAR
            value: n8nextraenvvar
        template: statefulset.yaml

  - it: should set extra environment variables when main.extraEnvVars is set
    set:
      main:
        extraEnvVars:
          N8N_EXTRA_ENV_VAR: n8nextraenvvar
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_EXTRA_ENV_VAR
            value: n8nextraenvvar
        template: statefulset.yaml

  - it: should set valueFrom environment variables when extraEnv is set
    set:
      extraEnv:
        - name: N8N_VALUE_FROM_VAR
          valueFrom:
            secretKeyRef:
              name: my-secret
              key: my-key
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_VALUE_FROM_VAR
            valueFrom:
              secretKeyRef:
                name: my-secret
                key: my-key
        template: statefulset.yaml

  - it: should set valueFrom environment variables when main.extraEnv is set
    set:
      main:
        extraEnv:
          - name: N8N_VALUE_FROM_VAR
            valueFrom:
              secretKeyRef:
                name: my-secret
                key: my-key
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_VALUE_FROM_VAR
            valueFrom:
              secretKeyRef:
                name: my-secret
                key: my-key
        template: statefulset.yaml

  - it: should set database configurations from database configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            configMapRef:
              name: n8n-database-configmap
        template: statefulset.yaml

  - it: should set logging configurations from logging configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            configMapRef:
              name: n8n-logging-configmap
        template: statefulset.yaml

  - it: should set diagnostics configurations from diagnostics configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            configMapRef:
              name: n8n-diagnostics-configmap
        template: statefulset.yaml

  - it: should set version notifications configurations from version notifications configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            configMapRef:
              name: n8n-version-notifications-configmap
        template: statefulset.yaml

  - it: should set public api configurations from public api configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            configMapRef:
              name: n8n-public-api-configmap
        template: statefulset.yaml

  - it: should set workflow history configurations from workflow history configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            configMapRef:
              name: n8n-workflow-history-configmap
        template: statefulset.yaml

  - it: should set task broker configurations from task broker configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            configMapRef:
              name: n8n-task-broker-configmap
        template: statefulset.yaml

  - it: should set metrics configurations from metrics configmap
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            configMapRef:
              name: n8n-metrics-configmap
        template: statefulset.yaml

  - it: should set encryption key from encryption key secret
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            secretRef:
              name: n8n-encryption-key-secret-v2
        template: statefulset.yaml

  - it: should set queue configurations from queue configmap when db type is postgresdb and worker mode is queue
    set:
      db:
        type: postgresdb
      worker:
        mode: queue
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            configMapRef:
              name: n8n-queue-configmap
        template: statefulset.yaml

  - it: should set queue configurations from queue configmap when db type is postgresdb and webhook mode is queue
    set:
      db:
        type: postgresdb
      webhook:
        mode: queue
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            configMapRef:
              name: n8n-queue-configmap
        template: statefulset.yaml

  - it: should set extra secret names for envFrom when extraSecretNamesForEnvFrom is set
    set:
      extraSecretNamesForEnvFrom:
        - secret1
        - secret2
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            secretRef:
              name: secret1
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            secretRef:
              name: secret2
        template: statefulset.yaml

  - it: should set extra secret names for envFrom when main.extraSecretNamesForEnvFrom is set
    set:
      main:
        extraSecretNamesForEnvFrom:
          - secret1
          - secret2
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            secretRef:
              name: secret1
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            secretRef:
              name: secret2
        template: statefulset.yaml

  - it: should set extra volume mounts when volumeMounts is set
    set:
      volumeMounts:
        - name: fake-volume
          mountPath: /fake-path
          readOnly: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: fake-volume
            mountPath: /fake-path
            readOnly: true
        template: statefulset.yaml

  - it: should set extra volume mounts when main.volumeMounts is set
    set:
      main:
        volumeMounts:
          - name: fake-volume
            mountPath: /fake-path
            readOnly: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: fake-volume
            mountPath: /fake-path
            readOnly: true
        template: statefulset.yaml

  - it: should set extra volumes when volumes is set
    set:
      volumes:
        - name: foo
          secret:
            secretName: mysecret
            optional: false
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: foo
            secret:
              secretName: mysecret
              optional: false
        template: statefulset.yaml

  - it: should set extra volumes when main.volumes is set
    set:
      main:
        volumes:
          - name: foo
            secret:
              secretName: mysecret
              optional: false
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: foo
            secret:
              secretName: mysecret
              optional: false
        template: statefulset.yaml

  - it: should set CA certificate volume and volumeMount when db.type postgresdb, postgresdb.ssl.enabled, and postgresdb.ssl.base64EncodedCertificateAuthorityFile are set
    set:
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            base64EncodedCertificateAuthorityFile: Q0EgZmlsZSBjb250ZW50
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: pg-ssl-ca
            mountPath: /home/node/certs/ca/ca.crt
            subPath: ca.crt
            readOnly: true
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pg-ssl-ca
            secret:
              secretName: n8n-pg-ssl-ca
              items:
                - key: ca.crt
                  path: ca.crt
          any: true
        template: statefulset.yaml

  - it: should set CA certificate volume and volumeMount when db.type postgresdb, postgresdb.ssl.enabled, and postgresdb.ssl.existingCertificateAuthorityFileSecret.name are set
    set:
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            existingCertificateAuthorityFileSecret:
              name: ca-cert-secret
              key: root.cert
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: pg-ssl-ca
            mountPath: /home/node/certs/ca/ca.crt
            subPath: ca.crt
            readOnly: true
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pg-ssl-ca
            secret:
              secretName: ca-cert-secret
              items:
                - key: root.cert
                  path: ca.crt
          any: true
        template: statefulset.yaml

  - it: should set certificate volume and volumeMount when db.type postgresdb, postgresdb.ssl.enabled, and postgresdb.ssl.base64EncodedCertFile are set
    set:
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            base64EncodedCertFile: Y2VydGlmaWNhdGUgZmlsZSBjb250ZW50
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: pg-ssl-cert
            mountPath: /home/node/certs/cert/cert.crt
            subPath: cert.crt
            readOnly: true
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pg-ssl-cert
            secret:
              secretName: n8n-pg-ssl-cert
              items:
                - key: cert.crt
                  path: cert.crt
          any: true
        template: statefulset.yaml

  - it: should set certificate volume and volumeMount when db.type postgresdb, postgresdb.ssl.enabled, and postgresdb.ssl.existingCertFileSecret.name are set
    set:
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            existingCertFileSecret:
              name: my-cert-secret
              key: client.cert
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: pg-ssl-cert
            mountPath: /home/node/certs/cert/cert.crt
            subPath: cert.crt
            readOnly: true
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pg-ssl-cert
            secret:
              secretName: my-cert-secret
              items:
                - key: client.cert
                  path: cert.crt
          any: true
        template: statefulset.yaml

  - it: should set certificate private key volume and volumeMount when db.type postgresdb, postgresdb.ssl.enabled, and postgresdb.ssl.base64EncodedPrivateKeyFile are set
    set:
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            base64EncodedPrivateKeyFile: Y2VydGlmaWNhdGUgcHJpdmF0ZSBrZXkgZmlsZSBjb250ZW50
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: pg-ssl-key
            mountPath: /home/node/certs/key/cert.key
            subPath: cert.key
            readOnly: true
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pg-ssl-key
            secret:
              secretName: n8n-pg-ssl-key
              items:
                - key: cert.key
                  path: cert.key
          any: true
        template: statefulset.yaml

  - it: should set certificate private key volume and volumeMount when db.type postgresdb, postgresdb.ssl.enabled, and postgresdb.ssl.existingPrivateKeyFileSecret.name are set
    set:
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            existingPrivateKeyFileSecret:
              name: my-cert-private-key-secret
              key: client.key
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: pg-ssl-key
            mountPath: /home/node/certs/key/cert.key
            subPath: cert.key
            readOnly: true
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: pg-ssl-key
            secret:
              secretName: my-cert-private-key-secret
              items:
                - key: client.key
                  path: cert.key
          any: true
        template: statefulset.yaml

  - it: should use private npm registry when only npmRegistry.enabled is true
    set:
      npmRegistry:
        enabled: true
        url: "https://my-registry.com"
        secretName: "n8n-npm-registry-secret"
        secretKey: "npmrc"
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: npmrc
            secret:
              optional: true
              secretName: n8n-npm-registry-secret
          any: true
        template: statefulset.yaml

  - it: should set node selector when nodeSelector is set
    set:
      nodeSelector:
        disktype: ssd
    asserts:
      - equal:
          path: spec.template.spec.nodeSelector.disktype
          value: ssd
        template: statefulset.yaml

  - it: should set affinity when affinity is set
    set:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 1
              preference:
                matchExpressions:
                  - key: disktype
                    operator: In
                    values:
                      - ssd
    asserts:
      - contains:
          path: spec.template.spec.affinity.nodeAffinity.preferredDuringSchedulingIgnoredDuringExecution
          content:
            weight: 1
            preference:
              matchExpressions:
                - key: disktype
                  operator: In
                  values:
                    - ssd
        template: statefulset.yaml

  - it: should set affinity when main.affinity is set
    set:
      main:
        affinity:
          podAntiAffinity:
            requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app.kubernetes.io/instance: n8n
              topologyKey: kubernetes.io/hostname
    asserts:
      - contains:
          path: spec.template.spec.affinity.podAntiAffinity.requiredDuringSchedulingIgnoredDuringExecution
          content:
            labelSelector:
              matchLabels:
                app.kubernetes.io/instance: n8n
            topologyKey: kubernetes.io/hostname
        template: statefulset.yaml

  - it: should set tolerations when tolerations are set
    set:
      tolerations:
        - key: fake-key
          operator: fake-operator
          value: fake-value
    asserts:
      - contains:
          path: spec.template.spec.tolerations
          content:
            key: fake-key
            operator: fake-operator
            value: fake-value
        template: statefulset.yaml

  - it: should set task runner container when taskRunners.mode is external
    set:
      taskRunners:
        mode: external
        external:
          port: 1234
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].image
          value: n8nio/runners:1.0.0
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].imagePullPolicy
          value: IfNotPresent
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].command
          value: ["/usr/local/bin/task-runner-launcher"]
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].args
          value: ["javascript"]
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].ports[0].containerPort
          value: 1234
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].ports[0].protocol
          value: TCP
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].livenessProbe.httpGet.path
          value: /healthz
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].livenessProbe.httpGet.port
          value: runner-health
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].readinessProbe.httpGet.path
          value: /healthz
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].readinessProbe.httpGet.port
          value: runner-health
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].startupProbe.httpGet.path
          value: /healthz
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].startupProbe.httpGet.port
          value: runner-health
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].startupProbe.failureThreshold
          value: 30
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].resources
          value:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].env
          content:
            name: N8N_RUNNERS_AUTH_TOKEN
            valueFrom:
              secretKeyRef:
                name: n8n-task-runners-secret
                key: auth-token
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].envFrom
          content:
            configMapRef:
              name: n8n-task-runners-configmap
        template: statefulset.yaml

  - it: should not add task runner sidecar on main pod when worker.mode is queue
    set:
      taskRunners:
        mode: external
      worker:
        mode: queue
      db:
        type: postgresdb
      main:
        forceToUseStatefulset: true
    asserts:
      - notContains:
          path: spec.template.spec.containers
          content:
            name: n8n-task-runners
        template: statefulset.yaml

  - it: should add python arg to task runner container when nodes.python.enabled is true
    set:
      taskRunners:
        mode: external
      nodes:
        python:
          enabled: true
      main:
        forceToUseStatefulset: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].args
          content: python
        template: statefulset.yaml

  - it: should not add python arg when nodes.python.enabled is false
    set:
      taskRunners:
        mode: external
      nodes:
        python:
          enabled: false
      main:
        forceToUseStatefulset: true
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].args
          value: ["javascript"]
        template: statefulset.yaml

  - it: should set security context on task runner container when taskRunners.mode is external
    set:
      taskRunners:
        mode: external
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].securityContext.runAsUser
          value: 1000
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].securityContext.runAsGroup
          value: 1000
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].securityContext.runAsNonRoot
          value: true
        template: statefulset.yaml

  - it: should mount tmp in task runner container when taskRunners.mode is external
    set:
      taskRunners:
        mode: external
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].volumeMounts
          content:
            name: tmp
            mountPath: /tmp
          any: true
        template: statefulset.yaml

  - it: should set seccompProfile RuntimeDefault in pod security context by default
    asserts:
      - equal:
          path: spec.template.spec.securityContext.seccompProfile.type
          value: RuntimeDefault
        template: statefulset.yaml

  - it: should always include tmp emptyDir volume
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: tmp
            emptyDir: {}
          any: true
        template: statefulset.yaml

  - it: should always mount tmp in main container
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: tmp
            mountPath: /tmp
          any: true
        template: statefulset.yaml

  - it: should always include cache emptyDir volume
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: cache
            emptyDir: {}
          any: true
        template: statefulset.yaml

  - it: should always mount cache in main container
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: cache
            mountPath: /home/node/.cache
          any: true
        template: statefulset.yaml

  - it: should set NPM_CONFIG_CACHE on npm-install init container when packages are set
    set:
      nodes:
        external:
          packages:
            - "moment@2.29.4"
    asserts:
      - contains:
          path: spec.template.spec.initContainers[?(@.name == "npm-install")].env
          content:
            name: NPM_CONFIG_CACHE
            value: /npmdata/.npm-cache
          any: true
        template: statefulset.yaml

  - it: should set NPM_CONFIG_CACHE on main container
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: NPM_CONFIG_CACHE
            value: /home/node/.cache/npm
          any: true
        template: statefulset.yaml

  - it: should always mount data dir in main container
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: node-modules
            mountPath: /home/node/.n8n
            readOnly: false
          any: true
        template: statefulset.yaml

  - it: should add license info command when license.enabled is true and license.activationKey is set
    set:
      license:
        enabled: true
        activationKey: fake-activation-key
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].command
          content:
            "/bin/sh"
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].args
          content:
            "-c"
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].args
          content:
            "n8n license:info && n8n"
          any: true
        template: statefulset.yaml

  - it: should add license info command when license.enabled is true and license.existingActivationKeySecret is set
    set:
      license:
        enabled: true
        existingActivationKeySecret: fake-existing-activation-key-secret
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].command
          content:
            "/bin/sh"
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].args
          content:
            "-c"
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].args
          content:
            "n8n license:info && n8n"
          any: true
        template: statefulset.yaml

  - it: should enable multi main setup when main.count is greater than 1
    set:
      main:
        count: 2
      worker:
        mode: queue
      webhook:
        mode: queue
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_MULTI_MAIN_SETUP_ENABLED
            value: "true"
          any: true
        template: statefulset.yaml

  - it: should not enable multi main setup when main.count is 1
    set:
      main:
        count: 1
    asserts:
      - notContains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_MULTI_MAIN_SETUP_ENABLED
            value: "true"
          any: true
        template: statefulset.yaml


  - it: should use license configmap and secret when license.enabled is true
    set:
      license:
        enabled: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            configMapRef:
              name: n8n-license-configmap
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].envFrom
          content:
            secretRef:
              name: n8n-license-activation-key
        template: statefulset.yaml

  - it: should set aws access key id from s3 object when binaryData.mode is s3 and minio is not enabled
    set:
      minio:
        enabled: false
      binaryData:
        mode: s3
        s3:
          accessKey: fake-access-key-id
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_EXTERNAL_STORAGE_S3_ACCESS_KEY
            valueFrom:
              secretKeyRef:
                key: access-key-id
                name: n8n-s3-secret
        template: statefulset.yaml

  - it: should set aws access key from rootUser key when binaryData.mode is s3 and minio is enabled and users are not set
    set:
      binaryData:
        mode: s3
      minio:
        enabled: true
        users: []
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_EXTERNAL_STORAGE_S3_ACCESS_KEY
            valueFrom:
              secretKeyRef:
                key: rootUser
                name: n8n-minio
        template: statefulset.yaml

  - it: should set aws access key from existing secret when binaryData.mode is s3 and minio is not enabled
    set:
      binaryData:
        mode: s3
        s3:
          existingSecret: fake-existing-secret
      minio:
        enabled: false
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_EXTERNAL_STORAGE_S3_ACCESS_KEY
            valueFrom:
              secretKeyRef:
                key: access-key-id
                name: fake-existing-secret
        template: statefulset.yaml

  - it: should set aws secret access key from s3 object when binaryData.mode is s3 and minio is not enabled
    set:
      minio:
        enabled: false
      binaryData:
        mode: s3
        s3:
          accessSecret: fake-secret-access-key
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_EXTERNAL_STORAGE_S3_ACCESS_SECRET
            valueFrom:
              secretKeyRef:
                key: secret-access-key
                name: n8n-s3-secret
        template: statefulset.yaml

  - it: should set aws secret access key from rootPassword key when binaryData.mode is s3 and minio is enabled and users are not set
    set:
      binaryData:
        mode: s3
      minio:
        enabled: true
        users: []
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_EXTERNAL_STORAGE_S3_ACCESS_SECRET
            valueFrom:
              secretKeyRef:
                key: rootPassword
                name: n8n-minio
        template: statefulset.yaml

  - it: should set aws secret access key from existing secret when binaryData.mode is s3 and minio is not enabled
    set:
      binaryData:
        mode: s3
        s3:
          existingSecret: fake-existing-secret
      minio:
        enabled: false
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_EXTERNAL_STORAGE_S3_ACCESS_SECRET
            valueFrom:
              secretKeyRef:
                key: secret-access-key
                name: fake-existing-secret
        template: statefulset.yaml

  - it: should set npm install init container with mount to internal task runner when nodes.external.packages is set and taskRunners.mode is internal
    set:
      taskRunners:
        mode: internal
      nodes:
        external:
          packages:
            - "moment@2.29.4"
    asserts:
      - contains:
          path: spec.template.spec.initContainers[?(@.name == "npm-install")].volumeMounts
          content:
            name: node-modules
            mountPath: "/npmdata"
            readOnly: false
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.initContainers[?(@.name == "npm-install")].volumeMounts
          content:
            name: node-modules
            mountPath: "/nodesdata/nodes"
            subPath: nodes
            readOnly: false
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: node-modules
            mountPath: "/home/node/.n8n"
            readOnly: false
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: NODE_FUNCTION_ALLOW_EXTERNAL
            value: "moment"
          any: true
        template: statefulset.yaml

  - it: should allow builtin node functions in internal task runner when nodes.builtin.modules is set and taskRunners.mode is internal
    set:
      taskRunners:
        mode: internal
      nodes:
        builtin:
          enabled: true
          modules:
            - "fs"
            - "crypto"
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: NODE_FUNCTION_ALLOW_BUILTIN
            value: "fs,crypto"
          any: true
        template: statefulset.yaml

  - it: should allow all builtin node functions in internal task runner when nodes.builtin.modules is enabled and taskRunners.mode is internal
    set:
      taskRunners:
        mode: internal
      nodes:
        builtin:
          enabled: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: NODE_FUNCTION_ALLOW_BUILTIN
            value: "*"
          any: true
        template: statefulset.yaml

  - it: should set npm install init container with mount to external task runner when nodes.external.packages is set and taskRunners.mode is external
    set:
      taskRunners:
        mode: external
      nodes:
        external:
          packages:
            - "moment@2.29.4"
    asserts:
      - contains:
          path: spec.template.spec.initContainers[?(@.name == "npm-install")].volumeMounts
          content:
            name: node-modules
            mountPath: "/npmdata"
            readOnly: false
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.initContainers[?(@.name == "npm-install")].volumeMounts
          content:
            name: node-modules
            mountPath: "/nodesdata/nodes"
            subPath: nodes
            readOnly: false
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].volumeMounts
          content:
            name: node-modules
            mountPath: "/home/node/.n8n"
            readOnly: false
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].env
          content:
            name: NODE_FUNCTION_ALLOW_EXTERNAL
            value: "moment"
          any: true
        template: statefulset.yaml

  - it: should allow all external npm packages when nodes.external.allowAll is true and taskRunners.mode is internal
    set:
      taskRunners:
        mode: internal
      nodes:
        external:
          allowAll: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: NODE_FUNCTION_ALLOW_EXTERNAL
            value: "*"
          any: true
        template: statefulset.yaml

  - it: should allow all external npm packages for external task runner when nodes.external.allowAll is true and taskRunners.mode is external
    set:
      taskRunners:
        mode: external
      nodes:
        external:
          allowAll: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].env
          content:
            name: NODE_FUNCTION_ALLOW_EXTERNAL
            value: "*"
          any: true
        template: statefulset.yaml

  - it: should allow builtin node functions in internal task runner when nodes.builtin.modules is set and taskRunners.mode is external
    set:
      taskRunners:
        mode: external
      nodes:
        builtin:
          enabled: true
          modules:
            - "fs"
            - "crypto"
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].env
          content:
            name: NODE_FUNCTION_ALLOW_BUILTIN
            value: "fs,crypto"
          any: true
        template: statefulset.yaml

  - it: should only keep npm packages and not community packages when nodes.external.packages is set and taskRunners.mode is internal
    set:
      taskRunners:
        mode: internal
      nodes:
        external:
          packages:
            - "moment@2.29.4"
            - "n8n-nodes-python@0.1.4"
            - "n8n-nodes-chatwoot@0.1.40"
    asserts:
      - equal:
          path: spec.template.spec.initContainers[?(@.name == "npm-install")].args[0]
          value: |
            export PACKAGES="moment@2.29.4 n8n-nodes-python@0.1.4 n8n-nodes-chatwoot@0.1.40"
            export COMMUNITY_PACKAGES="n8n-nodes-python@0.1.4 n8n-nodes-chatwoot@0.1.40"
            export NON_COMMUNITY_PACKAGES="moment@2.29.4"
            echo "$PACKAGES" | sha256sum > /npmdata/packages.hash.new
            if [ ! -f /npmdata/packages.hash ] || ! cmp /npmdata/packages.hash /npmdata/packages.hash.new; then
              if [ -n "$NON_COMMUNITY_PACKAGES" ]; then
                npm install --loglevel info --no-save --ignore-scripts $NON_COMMUNITY_PACKAGES --prefix /npmdata
              fi
              if [ -n "$COMMUNITY_PACKAGES" ]; then
                npm install --loglevel info --no-save --ignore-scripts $COMMUNITY_PACKAGES --prefix /nodesdata/nodes
              fi
              mv /npmdata/packages.hash.new /npmdata/packages.hash
            else
              rm /npmdata/packages.hash.new
            fi
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: NODE_FUNCTION_ALLOW_EXTERNAL
            value: "moment"
          any: true
        template: statefulset.yaml

  - it: should allow all builtin node functions in external task runner when nodes.builtin.modules is enabled and taskRunners.mode is external
    set:
      taskRunners:
        mode: external
      nodes:
        builtin:
          enabled: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].env
          content:
            name: NODE_FUNCTION_ALLOW_BUILTIN
            value: "*"
          any: true
        template: statefulset.yaml

  - it: should reinstall missing packages when nodes.external.reinstallMissingPackages is true and taskRunners.mode is internal
    set:
      taskRunners:
        mode: internal
      nodes:
        external:
          reinstallMissingPackages: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].env
          content:
            name: N8N_REINSTALL_MISSING_PACKAGES
            value: "true"
          any: true
        template: statefulset.yaml

  - it: should reinstall missing packages when nodes.external.reinstallMissingPackages is true and taskRunners.mode is external
    set:
      taskRunners:
        mode: external
      nodes:
        external:
          reinstallMissingPackages: true
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].env
          content:
            name: N8N_REINSTALL_MISSING_PACKAGES
            value: "true"
          any: true
        template: statefulset.yaml

  - it: should use private npm registry when external packages are set and npmRegistry.enabled is true
    set:
      nodes:
        external:
          packages:
            - "@mycompany/custom-n8n-functions@1.0.0"
      npmRegistry:
        enabled: true
        url: "https://my-registry.com"
        secretName: "n8n-npm-registry-secret"
        secretKey: "npmrc"
    asserts:
      - contains:
          path: spec.template.spec.initContainers[?(@.name == "npm-install")].volumeMounts
          content:
            name: npmrc
            mountPath: "/home/node/.npmrc"
            subPath: "npmrc"
            readOnly: true
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.initContainers[?(@.name == "npm-install")].volumeMounts
          content:
            name: node-modules
            mountPath: "/npmdata"
            readOnly: false
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.initContainers[?(@.name == "npm-install")].volumeMounts
          content:
            name: node-modules
            mountPath: "/nodesdata/nodes"
            subPath: nodes
            readOnly: false
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: npmrc
            secret:
              optional: true
              secretName: n8n-npm-registry-secret
          any: true
        template: statefulset.yaml

  - it: should add init container to main pod when main.initContainers is defined
    set:
      main:
        initContainers:
          - name: my-init-container
            image: "my-image:1.0.0"
            imagePullPolicy: IfNotPresent
            command:
              - sleep
              - infinity
    asserts:
      - equal:
          path: spec.template.spec.initContainers[0].name
          value: my-init-container
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.initContainers[0].image
          value: "my-image:1.0.0"
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.initContainers[0].imagePullPolicy
          value: IfNotPresent
        template: statefulset.yaml
      - equal:
          path: spec.template.spec.initContainers[0].command
          value:
            - sleep
            - infinity
        template: statefulset.yaml

  - it: should add extra containers to main pod when main.extraContainers is defined
    set:
      main:
        extraContainers:
          - name: my-extra-container
            image: "my-image:1.0.0"
            imagePullPolicy: IfNotPresent
            command:
              - sleep
              - infinity
    asserts:
      - contains:
          path: spec.template.spec.containers
          content:
            name: my-extra-container
            image: "my-image:1.0.0"
            imagePullPolicy: IfNotPresent
            command:
              - sleep
              - infinity
          any: true
        template: statefulset.yaml

  - it: should set dnsPolicy when dnsPolicy is set
    set:
      dnsPolicy: ClusterFirst
    asserts:
      - equal:
          path: spec.template.spec.dnsPolicy
          value: ClusterFirst
        template: statefulset.yaml

  - it: should set dnsConfig when dnsConfig is set
    set:
      dnsConfig:
        nameservers:
          - 1.1.1.1
          - 1.0.0.1
    asserts:
      - contains:
          path: spec.template.spec.dnsConfig.nameservers
          content:
            1.1.1.1
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.dnsConfig.nameservers
          content:
            1.0.0.1
          any: true
        template: statefulset.yaml

  - it: should set dnsPolicy and dnsConfig when dnsPolicy and dnsConfig are set
    set:
      dnsPolicy: None
      dnsConfig:
        nameservers:
          - 1.1.1.1
          - 1.0.0.1
    asserts:
      - equal:
          path: spec.template.spec.dnsPolicy
          value: None
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.dnsConfig.nameservers
          content:
            1.1.1.1
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.dnsConfig.nameservers
          content:
            1.0.0.1
          any: true
        template: statefulset.yaml

  - it: should add hostAliases to main pod when main.hostAliases is defined
    set:
      main:
        hostAliases:
          - ip: 1.1.1.1
            hostnames:
              - "my-host.com"
    asserts:
      - contains:
          path: spec.template.spec.hostAliases
          content:
            ip: 1.1.1.1
            hostnames:
              - "my-host.com"
          any: true
        template: statefulset.yaml

  - it: should use volume name when volumeName is set and taskRunners.mode is internal
    set:
      nodes:
        external:
          packages:
            - fake-package
      main:
        persistence:
          enabled: true
          volumeName: fake-volume-name
      taskRunners:
        mode: internal
    asserts:
      - contains:
          path: spec.template.spec.initContainers[?(@.name == "npm-install")].volumeMounts
          content:
            name: fake-volume-name
            mountPath: /npmdata
            readOnly: false
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-main")].volumeMounts
          content:
            name: fake-volume-name
            mountPath: /home/node/.n8n
            readOnly: false
          any: true
        template: statefulset.yaml
      - equal:
          path: spec.volumeClaimTemplates[?(@.metadata.name == "fake-volume-name")].metadata.name
          value: fake-volume-name
        template: statefulset.yaml

  - it: should use volume name when volumeName is set and taskRunners.mode is external
    set:
      nodes:
        external:
          packages:
            - fake-package
      main:
        persistence:
          enabled: true
          volumeName: fake-volume-name
      taskRunners:
        mode: external
    asserts:
      - contains:
          path: spec.template.spec.initContainers[?(@.name == "npm-install")].volumeMounts
          content:
            name: fake-volume-name
            mountPath: /npmdata
            readOnly: false
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].volumeMounts
          content:
            name: fake-volume-name
            mountPath: /home/node/.n8n
            readOnly: false
          any: true
        template: statefulset.yaml
      - equal:
          path: spec.volumeClaimTemplates[?(@.metadata.name == "fake-volume-name")].metadata.name
          value: fake-volume-name
        template: statefulset.yaml

  - it: should set runtimeClassName when main.runtimeClassName is defined
    set:
      main:
        runtimeClassName: "my-runtime-class"
    asserts:
      - equal:
          path: spec.template.spec.runtimeClassName
          value: my-runtime-class
        template: statefulset.yaml

  - it: should not set revisionHistoryLimit when revisionHistoryLimit is not set
    asserts:
      - notExists:
          path: spec.revisionHistoryLimit
        template: statefulset.yaml

  - it: should set revisionHistoryLimit when revisionHistoryLimit is set
    set:
      revisionHistoryLimit: 3
    asserts:
      - equal:
          path: spec.revisionHistoryLimit
          value: 3
        template: statefulset.yaml

  - it: should set revisionHistoryLimit to 0 when revisionHistoryLimit is 0
    set:
      revisionHistoryLimit: 0
    asserts:
      - equal:
          path: spec.revisionHistoryLimit
          value: 0
        template: statefulset.yaml

  - it: should match snapshot of default values
    release:
      name: n8n
      namespace: n8n
    chart:
      version: 1.0.0
      appVersion: 1.0.0
    set:
      encryptionKey: "test-encryption-key"
    asserts:
      - matchSnapshot: { }

  - it: should match snapshot of postgres ssl certificate values
    release:
      name: n8n
      namespace: n8n
    chart:
      version: 1.0.0
      appVersion: 1.0.0
    set:
      encryptionKey: "test-encryption-key"
      db:
        type: postgresdb
        postgresdb:
          ssl:
            enabled: true
            existingCertificateAuthorityFileSecret:
              name: my-ca-cert-secret
              key: root.crt
            existingCertFileSecret:
              name: my-client-cert-secret
              key: client.crt
            existingPrivateKeyFileSecret:
              name: my-client-cert-private-key-secret
              key: client.key
    asserts:
      - matchSnapshot: { }

  - it: should set runner-health port name on task runner sidecar container
    set:
      taskRunners:
        mode: external
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].ports[0].name
          value: runner-health
        template: statefulset.yaml

  - it: should use explicit tag when taskRunners.external.image.tag is set
    set:
      taskRunners:
        mode: external
        external:
          image:
            tag: "2.0.0"
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].image
          value: n8nio/runners:2.0.0
        template: statefulset.yaml

  - it: should use uv install command when nodes.python.external.packages is set
    set:
      taskRunners:
        mode: external
      nodes:
        python:
          enabled: true
          external:
            packages:
              - pandas
              - numpy
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].command
          value: ["/bin/sh", "-c", "UV_CACHE_DIR=/tmp/.uv-cache UV_LINK_MODE=copy uv pip install --target /home/node/.python-packages pandas numpy && export PYTHONPATH=/home/node/.python-packages && exec /usr/local/bin/task-runner-launcher javascript python"]
        template: statefulset.yaml

  - it: should use direct launcher command when nodes.python.external.packages is empty
    set:
      taskRunners:
        mode: external
      nodes:
        python:
          enabled: true
          external:
            packages: []
    asserts:
      - equal:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].command
          value: ["/usr/local/bin/task-runner-launcher"]
        template: statefulset.yaml

  - it: should mount python-packages volume in StatefulSet runner sidecar at /home/node/.python-packages
    set:
      taskRunners:
        mode: external
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].volumeMounts
          content:
            name: python-packages
            mountPath: /home/node/.python-packages
        template: statefulset.yaml

  - it: should not add python-packages volume in StatefulSet when taskRunners.mode is internal
    set:
      taskRunners:
        mode: internal
    asserts:
      - notContains:
          path: spec.template.spec.volumes
          content:
            name: python-packages
            emptyDir: {}
        template: statefulset.yaml

  - it: should add python-packages emptyDir in StatefulSet volumes when persistence is disabled
    set:
      taskRunners:
        mode: external
      nodes:
        python:
          persistence:
            enabled: false
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: python-packages
            emptyDir: {}
        template: statefulset.yaml

  - it: should add python-packages to StatefulSet volumeClaimTemplates when persistence is enabled
    set:
      taskRunners:
        mode: external
      nodes:
        python:
          persistence:
            enabled: true
            accessMode: ReadWriteOnce
            size: 2Gi
    asserts:
      - contains:
          path: spec.volumeClaimTemplates
          content:
            metadata:
              name: python-packages
            spec:
              accessModes:
                - ReadWriteOnce
              resources:
                requests:
                  storage: 2Gi
        template: statefulset.yaml
      - notContains:
          path: spec.template.spec.volumes
          content:
            name: python-packages
            emptyDir: {}
        template: statefulset.yaml

  - it: should use existingClaim as regular volume in StatefulSet when existingClaim is set
    set:
      taskRunners:
        mode: external
      nodes:
        python:
          persistence:
            enabled: true
            existingClaim: my-existing-pvc
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: python-packages
            persistentVolumeClaim:
              claimName: my-existing-pvc
        template: statefulset.yaml
      - notContains:
          path: spec.volumeClaimTemplates
          content:
            metadata:
              name: python-packages
        template: statefulset.yaml

  - it: should not add python-packages volume in main StatefulSet when worker.mode is queue
    set:
      taskRunners:
        mode: external
      worker:
        mode: queue
      main:
        count: 2
    asserts:
      - notContains:
          path: spec.template.spec.volumes
          content:
            name: python-packages
            emptyDir: {}
        template: statefulset.yaml
      - notContains:
          path: spec.volumeClaimTemplates
          content:
            metadata:
              name: python-packages
        template: statefulset.yaml

  - it: should set UV_DEFAULT_INDEX when pypiRegistry.enabled and url is set without config file
    set:
      taskRunners:
        mode: external
      main:
        count: 2
      pypiRegistry:
        enabled: true
        url: "https://my.registry.com/simple/"
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].env
          content:
            name: UV_DEFAULT_INDEX
            value: "https://my.registry.com/simple/"
          any: true
        template: statefulset.yaml

  - it: should set UV_CONFIG_FILE when pypiRegistry.enabled and customUvConfig is set
    set:
      taskRunners:
        mode: external
      main:
        count: 2
      pypiRegistry:
        enabled: true
        customUvConfig: "[index]\nurl = \"https://my.registry.com/simple/\""
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].env
          content:
            name: UV_CONFIG_FILE
            value: "/home/node/.uv-config/uv.toml"
          any: true
        template: statefulset.yaml

  - it: should set UV_CONFIG_FILE when pypiRegistry.enabled and secretName is set
    set:
      taskRunners:
        mode: external
      main:
        count: 2
      pypiRegistry:
        enabled: true
        secretName: "my-uv-secret"
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].env
          content:
            name: UV_CONFIG_FILE
            value: "/home/node/.uv-config/uv.toml"
          any: true
        template: statefulset.yaml

  - it: should mount uv-config volume when pypiRegistry.enabled and customUvConfig is set
    set:
      taskRunners:
        mode: external
      main:
        count: 2
      pypiRegistry:
        enabled: true
        customUvConfig: "[index]\nurl = \"https://my.registry.com/simple/\""
    asserts:
      - contains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].volumeMounts
          content:
            name: uv-config
            mountPath: /home/node/.uv-config
            readOnly: true
          any: true
        template: statefulset.yaml
      - contains:
          path: spec.template.spec.volumes
          content:
            name: uv-config
            secret:
              secretName: "n8n-pypi-registry-secret"
              optional: true
          any: true
        template: statefulset.yaml

  - it: should use existing secret name for uv-config volume when pypiRegistry.secretName is set
    set:
      taskRunners:
        mode: external
      main:
        count: 2
      pypiRegistry:
        enabled: true
        secretName: "my-existing-uv-secret"
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: uv-config
            secret:
              secretName: "my-existing-uv-secret"
              optional: true
          any: true
        template: statefulset.yaml

  - it: should not add pypiRegistry env vars when pypiRegistry is disabled
    set:
      taskRunners:
        mode: external
      main:
        count: 2
      pypiRegistry:
        enabled: false
        url: "https://my.registry.com/simple/"
    asserts:
      - notContains:
          path: spec.template.spec.containers[?(@.name == "n8n-task-runners")].env
          content:
            name: UV_DEFAULT_INDEX
            value: "https://my.registry.com/simple/"
          any: true
        template: statefulset.yaml

  - it: should not add uv-config volume when pypiRegistry is disabled
    set:
      taskRunners:
        mode: external
      main:
        count: 2
      pypiRegistry:
        enabled: false
        customUvConfig: "[index]\nurl = \"https://my.registry.com/simple/\""
    asserts:
      - notContains:
          path: spec.template.spec.volumes
          content:
            name: uv-config
            secret:
              secretName: "n8n-pypi-registry-secret"
              optional: true
          any: true
        template: statefulset.yaml

  - it: should not add uv-config volume in main StatefulSet when worker.mode is queue
    set:
      taskRunners:
        mode: external
      worker:
        mode: queue
      main:
        count: 2
      pypiRegistry:
        enabled: true
        customUvConfig: "[index]\nurl = \"https://my.registry.com/simple/\""
    asserts:
      - notContains:
          path: spec.template.spec.volumes
          content:
            name: uv-config
            secret:
              secretName: "n8n-pypi-registry-secret"
              optional: true
          any: true
        template: statefulset.yaml

  - it: should add community-node-modules to volumeClaimTemplates when forceToUseStatefulset and community persistence enabled
    set:
      main:
        forceToUseStatefulset: true
        persistence:
          enabled: false
      nodes:
        external:
          packages:
            - "n8n-nodes-evolution-api"
          persistence:
            enabled: true
            accessMode: ReadWriteOnce
            size: 2Gi
    asserts:
      - contains:
          path: spec.volumeClaimTemplates
          content:
            metadata:
              name: community-node-modules
            spec:
              accessModes:
                - ReadWriteOnce
              resources:
                requests:
                  storage: 2Gi
        template: statefulset.yaml
      - notContains:
          path: spec.template.spec.volumes
          content:
            name: community-node-modules
            emptyDir: {}
        template: statefulset.yaml

  - it: should use existingClaim as regular volume in StatefulSet when community existingClaim is set
    set:
      main:
        forceToUseStatefulset: true
        persistence:
          enabled: false
      nodes:
        external:
          packages:
            - "n8n-nodes-evolution-api"
          persistence:
            enabled: true
            existingClaim: my-existing-community-pvc
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: community-node-modules
            persistentVolumeClaim:
              claimName: my-existing-community-pvc
        template: statefulset.yaml
      - notContains:
          path: spec.volumeClaimTemplates
          content:
            metadata:
              name: community-node-modules
        template: statefulset.yaml

  - it: should add community-node-modules emptyDir in StatefulSet when forceToUseStatefulset and community persistence disabled
    set:
      main:
        forceToUseStatefulset: true
        persistence:
          enabled: false
      nodes:
        external:
          packages:
            - "n8n-nodes-evolution-api"
          persistence:
            enabled: false
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: community-node-modules
            emptyDir: {}
        template: statefulset.yaml
